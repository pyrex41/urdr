# M1 status

Status: **abstract-world exit demonstration PASS on shen-go** (Wave I
integration suite). Milestone modules A–I are present in-tree; Bifrost
cases `explore-shrink`, `scenario-grammar`, and `m1-integration` are
wired with pinned goldens. Four-port re-pin (agent 23) is **not** claimed
for Waves E–I. No whole-system determinism profile is claimed.

## What is claimed

- Modeled abstract world: component interface, scenario DSL, NetKAT
  fragment, NetworkPolicy compiler (`spec-semantics-only`), properties,
  event log / replay / modeled-run certificates, seeded exploration and
  shrinking.
- Wave I reference scenario `scenarios/abstract/partition-retry.shen`
  validates under the scenario DSL (three processes, partition fault,
  liveness deadline).
- Seven-item exit demonstration (`shen/tests/integration/`) on **shen-go**:
  1. `explore` discovers the injected partition failure from a seed
  2. `shrink` removes at least one irrelevant choice; failure persists
  3. `replay` of the minimized artifact reproduces the failure/verdicts
  4. NetKAT isolation: partition policy yields symbolic `unreachable`;
     open-fabric reachability witness is `confirmed` (not `unconfirmed`)
  5. NetworkPolicy: default-deny-all + multi-peer (`test-network-policy`)
     compiles with isolation claim; over-permissive allow-all-ingress
     admits an unintended flow (concrete admission witness)
  6. Modeled-run certificate: zero non-`pure`/`modeled` events
     (`abstract=true`), markers `modeled-world-only` and
     `spec-semantics-only`
  7. `make quality` / full four-port conformance: **foreman-owned**
     (printed deferred in the suite; not run by this agent)

## What is not claimed

- No D0–D3 whole-system profile.
- Certificates are **modeled-world-only**; they do not certify guest,
  Firecracker, UAP-transport, or live CNI paths.
- NetworkPolicy results are **spec-semantics-only** (ADR 0004 Decision 4);
  no CNI fidelity claim (M4).
- Prolog is **not certifying** (Wave G spike / fallback; not required for
  M1 exit).
- Cross-runtime byte-identity for Waves E–I suites awaits the four-port
  sweep (agent 23). Waves A–D / C2 retain their prior four-port evidence
  where recorded at merge.
- Four-port byte-identity for Waves E–I remains open until agent 23.

## Accepted production modules

| Area | Location | ADR / notes |
| --- | --- | --- |
| Canonical values + netstring framing | `shen/protocol/canonical.shen`, `protocol/fixtures/v1/canonical/` | ADR 0002, ADR 0005 profiles |
| Software integers + named SHA streams | `shen/world/integer.shen`, `shen/world/prng.shen` | ADR 0003 |
| UAP v1 boundary | `protocol/uap.md`, `adapters/common/uap/` | M0; no M1 transport work |
| Component interface + world reducer | `shen/world/component.shen`, `shen/world/world.shen` | Wave A; ADR 0004 D1 |
| Scenario DSL | `shen/scenario/scenario.shen`, `protocol/fixtures/v1/scenario/` | Wave B |
| NetKAT fragment | `shen/world/netkat.shen`, `protocol/fixtures/v1/netkat/` | Wave C; ADR 0004 D2 |
| Abstract models (net/timer/fault) | `shen/world/models/`, `shen/world/netkat-witness.shen` | Wave C |
| NetworkPolicy compiler | `shen/world/netpol.shen`, `protocol/fixtures/v1/netpol/` | Wave C2; ADR 0004 D4 |
| Properties / verdicts | `shen/properties/properties.shen` | Wave D |
| Event log, replay, certificates | `shen/world/eventlog.shen`, `replay.shen`, `certificate.shen` | Wave E |
| Seeded search | `shen/search/search.shen` | Wave F; SPEC.md §17 |
| Transcript shrink | `shen/shrink/shrink.shen` | Wave F; failure-signature gate |
| Scenario grammar | `shen/search/grammar.shen` | Wave H; ADR 0004 D5 |
| Partition-retry scenario | `scenarios/abstract/partition-retry.shen` | Wave I reference |
| Exit demonstration | `shen/tests/integration/` | Wave I |

## Gate evidence notes

Development mode (from Wave E onward, per `docs/plans/m1-agent-team.md`):

- Per-wave merge evidence is **single-port** (this Wave I run: **shen-go**).
- Waves A–D and C2 retain recorded four-port evidence from their merges.
- Bifrost suite wiring for M1 exit cases is done (see golden digests
  below). Four-port re-pin remains agent 23.

Commands for the exit demonstration (this agent):

```text
/Users/reuben/.local/bin/shen-go script shen/tests/integration/run-tests.shen
# or: shen/tests/integration/test.sh
scripts/check-owned-paths --base-ref a46e1b6ad62a176ed0c3f9f96467a918ac094111 \
  --allow 'scenarios/abstract/' --allow 'docs/status/m1.md' \
  --allow 'shen/tests/integration/'
```

Observed (2026-07-27, shen-go only):

- Integration suite: **ALL PASS** (8 DEMO lines + cases count)
- Explore-shrink, scenario-grammar, integration: **shen-go exact-golden**
- Adversarial suite: **87 tests OK**
- `make quality`: **PASS** (fmt-check + 62 unit tests including gate mutations)
- Other ports / `make conformance` four-port: **not claimed** (agent 23)

Pinned dependency commits (from M0 status; not re-verified here):

| Dependency | Commit |
| --- | --- |
| Bifrost | `51d24d81f7c7a15d382f1c02238453693bb46158` |
| shen-go | `8775fc366945bfe863f0cb0e0af2ada901ae1819` |
| shen-lua | `c85ad7edc79e183918b67862b8a60fa17c7ac9b9` |
| shen-cl | `8df94be8365ab48561655673593c137f55f92cdb` |
| shen-rust | `a529924d7f9c16d839397148cf60747f736dd7b1` |

## Golden digests (shen-go)

File digests of checked-in suite goldens (SHA-256 of `golden.txt`):

| Suite | `golden.txt` SHA-256 |
| --- | --- |
| integration (Wave I) | `e73c5a5281e73a60480a74a6d969fc7efd82ff8dcc7eccf032412f73134b2d66` |
| search / shrink (Wave F) | `f724cb47f60856595a3e7683b68c7977cf2682d5661311b1605ac936d0ff6bed` |
| scenario grammar (Wave H) | `702eb0c7cca69a2fc85683d2266410a493228a5ba1de4b3f014ebf1166bbbc02` |
| replay / certificate (Wave E) | `046595da6c92a60bc2620fc3de8243e927ef07f64a7dbfe0897e6324542d6cba` |
| properties (Wave D) | `153b6b11171f0440924c88f4a7eb7119996578a0a7b47054a89e5b077f928479` |
| netpol (Wave C2) | `09b0be2f36e0175c592458d0ca9c5ebbb02f8077e7f1847ecf8f00bad5e8f6a1` |
| models / witness (Wave C) | `f3f7ae5b47c8eb26ae71ec21ba5eaafe43b800be6025f98b0f65231f08d56f9b` |
| netkat (Wave C) | `df30b741e25d54aef93e19868bff3fbf74bcc397df213de4e74fa4fd591d817e` |
| world / component (Wave A) | `9f8b8127c76c817b4d555abc786c7a97ff28d6e36ca819b62ebef1b2f20dccf3` |

Wave I integration semantic highlights (from `shen/tests/integration/golden.txt`):

| Item | Projection |
| --- | --- |
| scenario frame digest | `eb91fed0928a5b858e444912bdb470cc489381bbf201de71ae021615cda27c2c` |
| explore path digest | `cf1f3ecb98e5fa293405374cc7245deeb0deb19e772cd81f6aabff0842cecfd0` |
| shrink | before=14 after=1 persist=true |
| replay transcript root | `db65fba69941a791db933abd0adfa6ca3b657060377210e1cb2314ac7cce118d` |
| NetKAT | isolation=unreachable, witness=confirmed |
| netpol isolation | `IEIEIE----`, marker=spec-semantics-only, unintended index 0 |
| certificate run-id | `fa873f159d6f18dbc494555418ed3ead66495a302139f1bbdd82bfcc21f523ea` |

M0 bifrost case digests (unchanged reference):

| Case | Digest |
| --- | --- |
| canonical-values | `a4f545f90d62436c7560a754722fd331c063288a964bee3d19dea68e4ab00b25` |
| named-prng | `817e441b925f2b5239500c4c515e35b821f53a47f66fd8a3cc6b37523dcc9e4c` |
| world-reducer | `b1bbea1e7d5e1dbebd30130cf5f627e444287463fd62744b6dd0c72b56cc768f` |

## Bifrost cases (wired)

| Case | Golden path | SHA-256 |
| --- | --- | --- |
| explore-shrink | `shen/tests/search/golden.txt` | `f724cb47f60856595a3e7683b68c7977cf2682d5661311b1605ac936d0ff6bed` |
| scenario-grammar | `shen/tests/search/grammar/golden.txt` | `702eb0c7cca69a2fc85683d2266410a493228a5ba1de4b3f014ebf1166bbbc02` |
| m1-integration | `shen/tests/integration/golden.txt` | `e73c5a5281e73a60480a74a6d969fc7efd82ff8dcc7eccf032412f73134b2d66` |

Also: `shen/tests/search/grammar/golden.txt` suite digest above; grammar
program pin lives in `bifrost.suite.json`.

## Remaining work

1. Four-port sweep (agent 23): re-run every M1 suite on
   shen-go, shen-lua, shen-cl, shen-rust; re-pin any disagreement.
2. `make conformance REQUIRED_IMPLS=shen-go,shen-lua,shen-cl,shen-rust`
   after the sweep (requires built launchers for all four ports).

## Explicit non-claims (summary)

| Claim | Status |
| --- | --- |
| D0–D3 whole-system profile | **not claimed** |
| Modeled-world-only certificates | claimed for abstract runs |
| CNI / live NetworkPolicy fidelity | **not claimed** (`spec-semantics-only`) |
| Prolog as certifying surface | **not claimed** (Wave G fallback) |
| Four-port identity for E–I | **not claimed** until agent 23 |
| Bifrost suite cases for M1 exit | **wired** (single-port evidence) |

Hosted GitHub Actions remains infrastructure-blocked (billing); it is not
counted as evidence either way.
